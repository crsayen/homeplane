{
    # LAN-first TLS using Caddy's internal CA.
    local_certs
}

{$HOMEPLANE_HOSTS} {
    encode zstd gzip

    handle /go2rtc/* {
        uri strip_prefix /go2rtc
        reverse_proxy 192.168.0.124:1984
    }

    reverse_proxy web:80
}
